#!/usr/bin/env bash
set -euo pipefail
# Wrapper to run the Go kclone CLI from the workspace
REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
BIN_PATH="$REPO_ROOT/kclone"
if [ -x "$BIN_PATH" ]; then
  exec "$BIN_PATH" "$@"
else
  exec go run "$REPO_ROOT/go/cmd/kclone" -- "$@"
fi
